# Local Government Office — Placement Previews

This folder preserves representative generated layouts that have been visually reviewed against `../LEVEL_PROFILE.md`.

| Preview | Profile | Generation version | Seed | Review state |
| --- | --- | ---: | --- | --- |
| `LocalOffice_Placement_prototype-2.png` | `local_government_office` | `2` | `prototype-2` | Furnished playable reference; archive verified as one-entrance and windowless |

The saved image is a review snapshot, not a hand-authored level or runtime dependency. Recreate the current layout with:

`mission-placement-preview.html?seed=prototype-2`

If the profile or generator changes intentionally, increment its generation version and capture a new preview rather than silently replacing an older-version reference.
